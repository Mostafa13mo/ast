pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo '✅ Pipeline works perfectly!'
                sh 'echo "This is running on the Jenkins agent"'
            }
        }
    }
}
